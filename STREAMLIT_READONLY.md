# Streamlit 읽기 전용 Google Sheets 버전

이 버전은 Google Cloud, 서비스 계정, API 키를 사용하지 않습니다.

## 실행

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Google Sheets 구성

한 스프레드시트에 다음 탭을 사용합니다.

- `Holdings`: 현재 전략별 종목과 보유수량
- `Strategies`: 전략별 규칙과 파라미터
- `Snapshots`: 매월 말 포트폴리오 상태
- `Actions`: 리밸런싱 계획과 실제 실행 이력
- `Cashflows`: 입출금 원장
- `CategoryTargets`: 전체 자산군 목표비중

`Holdings` 탭의 첫 행은 다음 열 이름을 사용합니다.

Google Sheets의 첫 행에 다음 열 이름을 사용합니다.

```text
strategy account ticker name market category role target_pct shares
```

방법은 두 가지입니다.

1. 공유 권한을 **링크가 있는 모든 사용자: 뷰어**로 설정하고, 스프레드시트 URL을 앱 사이드바에 입력합니다.
2. 공개 링크를 사용하지 않으려면 시트 범위를 헤더와 함께 복사하여 앱의 붙여넣기 입력창에 넣습니다.

앱은 탭 이름으로 표를 한 번에 읽습니다. `Strategies`, `Snapshots`, `Actions`, `Cashflows`, `CategoryTargets`가 아직 없으면 기본 전략 또는 빈 기록으로 시작합니다.

## 매월 사용 순서

1. `Holdings`에 지난달 거래 후 실제 보유수량과 현금을 입력합니다. `CASH`의 `shares`는 원화 금액입니다.
2. 앱에서 기준일을 고르고 종가를 조회합니다. 휴장일이면 기준일 이전 최근 거래일과 실제 가격일이 함께 표시됩니다.
3. `월말 마감`에서 가격·환율·전략 설정 오류와 지난 기록 대비 평가액 변화를 확인합니다.
4. `대시보드`에서 전략별 현재비중과 자산군 구성을 확인합니다.
5. `리밸런싱 실행`에서 목표비중형 전략의 주문안을 검토합니다. `hold` 전략은 평가만 하고 주문을 만들지 않습니다.
6. 다음 거래일 체결 후 실제수량과 원화 실제체결금액을 입력하고 실행을 체크합니다.
7. `복사용 데이터`에서 `Snapshots`와 `Actions`를 누적하고, 생성된 다음 달 보유내역으로 `Holdings` 전체를 교체합니다.

같은 기준일의 `Snapshots`를 다시 붙이면 성과가 중복 집계되므로 기존 해당 날짜 행을 먼저 교체해야 합니다.

## 주요 기능

- 전략별 종목 구성과 목표비중 편집
- LAA SMA 필터, GSM 모멘텀, 낙폭 분할매수·비중전환, 보유 유지 규칙 설정
- 월말 종가 기준 리밸런싱 액션 계산
- 실제 사용 가격일·환율·누락 데이터 검증
- 지난 기록 대비 전략별 평가액 증감
- 체결 결과를 반영한 다음 달 Holdings 생성
- 자산군별 월별 평가액·비중 히스토리
- 전략별 현재 상태 및 리밸런싱 필요도 표시
- 목표 충족(초록), 미달(파랑), 초과(빨강) 비중 막대
- 전략 추가·삭제, 계좌명·설명·활성화, 구성 종목과 목표비중 편집
- 월별 스냅샷 및 실행 이력 조회
- CAGR, MDD, 연환산 변동성, Sharpe 계산
- QQQ, SPY, KOSPI200 및 사용자 티커 벤치마크 비교

## 결과 보관

`복사용 데이터` 탭에서 다음 표를 TSV로 복사하거나 CSV로 다운로드합니다.

- 월말 스냅샷
- 액션 및 실제 실행 이력
- 체결 반영 후 다음 달 보유내역
- 전략 설정
- 입출금 원장
- 자산군 목표비중

앱은 Google Sheets에 직접 데이터를 쓰지 않습니다.
